id: https://example.org/second
name: second_schema
description: Another test schema

classes:
  Person:
    slots:
      - id
      - email
  Address:
    slots:
      - street
      - city

slots:
  id:
    range: string
    identifier: true
  email:
    range: string
  street:
    range: string
  city:
    range: string
